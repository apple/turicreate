/**
 * \defgroup sgraph_physical SGraph : Physical
 *
 * The SGraph Physical Format describes how SGraphs are laid out on disk.
 * They are internally actually stored as SFrames, but laid out in a particular
 * way for ease of performing graph computation.
 *
 * For a user of the SFrame, the main collection of objects that matter is
 * in \ref sgraph_main
 */
